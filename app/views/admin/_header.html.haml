%header
  .container
    %h1 Magnetism
    %ul#settings
      %li= link_to current_user.name, '#'
      %li= link_to 'Settings', '#'
      %li= link_to 'Logout', logout_path
  #mainNav
    .container
      %ul.tab-collection
        %li= link_to 'Dashboard', user_root_path, :class => 'tab'
        %li= link_to 'Pages', admin_pages_path, :class => 'tab'
        %li= link_to 'Assets', '#', :class => 'tab'
        %li= link_to 'Manage', admin_manage_path, :class => 'tab'
      = render :partial => 'admin/shared/selector', :locals => { :id => 'site-selector', :name => current_site.name }
