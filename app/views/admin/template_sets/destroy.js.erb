var $template_set = $('#template-set-<%= resource.id %>');
$('<%= resource.templates.map { |t| "a.close[data-template-id=#{t.id}]" }.join(',') %>')
  .each(function(){ Template.close(this) });
$('#template-set-header-<%= resource.id %>').remove();
$template_set.remove();
