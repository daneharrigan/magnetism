#side-bar
  %h2.toggle-sibling Templates
  %ul#templates= render :partial => 'admin/templates/item', :collection => template_collection, :as => :template, :locals => { :theme => resource }

  %h2.toggle-sibling Snippets
  %ul#snippets= render :partial => 'admin/templates/item', :collection => snippet_collection, :as => :template, :locals => { :theme => resource }

  %h2.toggle-sibling Stylesheets
  %ul#stylesheets= render :partial => 'admin/templates/item', :collection => stylesheet_collection, :as => :template, :locals => { :theme => resource }

  %h2.toggle-sibling JavaScripts
  %ul#javascripts= render :partial => 'admin/templates/item', :collection => javascript_collection, :as => :template, :locals => { :theme => resource }